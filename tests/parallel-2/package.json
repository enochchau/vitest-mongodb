{
  "name": "vitest-mongodb-parallel-2",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "vitest",
    "test:nothread": "vitest --no-threads",
    "coverage": "vitest run --coverage"
  },
  "dependencies": {
    "mongodb": "^5.7.0",
    "vite": "^4.4.9",
    "vitest": "^0.34.1",
    "vitest-mongodb": "workspace:*"
  },
  "devDependencies": {
    "c8": "^8.0.1"
  }
}
