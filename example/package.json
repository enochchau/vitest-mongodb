{
  "name": "vitest-mongodb-test",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "coverage": "vitest run --coverage"
  },
  "dependencies": {
    "mongodb": "^4.8.1",
    "vite": "^3.0.4",
    "vitest": "^0.20.3",
    "vitest-mongodb": "0.0.1"
  },
  "devDependencies": {
    "c8": "^7.12.0"
  }
}
