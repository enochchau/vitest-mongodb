{
  "name": "vitest-mongodb-test",
  "private": true,
  "scripts": {
    "test": "vitest",
    "coverage": "vitest run --coverage"
  },
  "dependencies": {
    "vite": "^3.0.4",
    "vitest": "^0.20.3",
    "mongodb": "^4.8.1"
  }
}
